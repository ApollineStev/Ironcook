<section>
    <div class="album py-5 bg-light">

        <div class="mx-5">
            <h2> {{recipe.title}} </h2>
            <hr>
        </div>

        <div> 
            <p>Recipe by : {{username}}</p>
            <p>Description : {{recipe.description}}</p>
            <p>Ingredients : {{recipe.ingredients}}</p>
            <p>Cuisine : {{recipe.cuisine}}</p>
            <p>Dish type : {{recipe.dishType}}</p>
            <p>Difficulty : {{recipe.difficulty}}</p>
            <p>Cooking time : {{recipe.cookingTime}} min</p>
            <img src="{{recipe.imageUrl}}">

        </div>


            <div>
                <a href="/recipe/{{recipe._id}}/edit">
                    <button add product-id="{{id}}">Edit</button>
                </a>

                <form action="/recipe/{{recipe._id}}/delete" method="Post">
                    <button>Delet recipe</button>
                </form>
            </div>

        <!--       Commented for now I don't get the user thing...
        {{#if userInSession}}
            <div>
                <a href="/products/{{id}}/edit">
                    <button add product-id="{{id}}">Edit</button>
                </a>

                <a href="/recipe/{{id}}/delete">
                    <button remove recipe-id="{{id}}">Remove</button>
                </a>
            </div>
        {{/if}}-->












    </div>
    
</section>